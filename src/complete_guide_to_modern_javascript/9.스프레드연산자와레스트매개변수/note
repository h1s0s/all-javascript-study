//9.1 스프레드 연산자


//배열의 결합
const vaggie = ["tomato", "cucumber", "beans"];
const meat = ["pork", "beef", "chicken"];
const menu = [...veggie, "pasta", ...meat];

//배열의 복사본 만들기
const newVaggie = [].concat(vaggie);
const newVaggie2 = [...vaggie];

//함수의 스프레드 연산자
function doStuff (x, y, z){
    console.log(x+y+z)
}
doStuff(...arg);

//객체 복사
let clone = {...vaggie}

//9.2 레스트 매개변수
const runners = ["tom", "paul", "mark", "luke"]
const [first, second, ...losers] = runners;